<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme (the actual UI is drawn by Jetpack Compose). -->
    <style name="Theme.UploadGo" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme (AndroidX SplashScreen compat, works on all API levels). -->
    <style name="Theme.UploadGo.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.UploadGo</item>
    </style>

</resources>
